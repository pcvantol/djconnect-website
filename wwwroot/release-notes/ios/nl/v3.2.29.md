# DJConnect App 3.2.29

## Gewijzigd

- Apple-clients bewaren nu `ha_install_id`, `integration_version` en optioneel
  `pairing_session_id` uit Home Assistant pairing/status responses en sturen
  die metadata mee naar Central zonder APNs-token.

## Opgelost

- Watch push-bootstrap-herstel loopt nu via de iPhone-proxy met de Watch
  identity, en probeert Home Assistant daarna exact één keer opnieuw met een
  Central `djcboot_...` proof.
- Central bootstrap-fouten zoals `invalid_client_type`,
  `invalid_app_bundle_id`, `invalid_push_environment` en
  `bootstrap_rate_limited` worden nu als privacy-veilige statuscodes bewaard.
- Track Insight op Watch vult energie/detailmetrics nu uit geneste backend
  payloads, en Music DNA vraagt niet opnieuw om activeren wanneer het al aan
  staat.

---

Platform: iOS

Niet-ondertekende CI-artifacts zijn bedoeld voor diagnostics en interne validatie.
Niet-ondertekende iOS-binaries worden hier gepubliceerd voor diagnostics en interne validatie. Gebruik voor normale iOS-distributie TestFlight/App Store.
